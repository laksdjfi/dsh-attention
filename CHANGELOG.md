# Changelog

## [0.2.0] - 2026-08-24

### Added
- **完成通知**：后台会话任务跑完（未被选中期间运行结束，DSH 内置 `completed` 标记点亮）时，也弹出系统通知；点击同样跳转到对应会话。
- 设置分区新增「后台任务完成时也发送通知」独立开关（`notifyCompleted`，默认开）。

## [0.1.0] - 2026-08-24

### Added
- 系统通知提醒：任意会话出现待处理**审批**（权限越权执行）或**提问**（`ask_user_question`）时，通过浏览器 `Notification` API 弹出系统通知（标签页在后台/窗口最小化也能收到）。
- 点击通知自动聚焦 DSH 窗口并选中对应会话，落地到内置的审批/提问卡片处理。
- 防打扰：页面聚焦且正在该会话时跳过提醒（内置卡片已可见）。
- 设置分区（设置 → 确认提醒）：启用开关、通知权限授权、测试通知；偏好存 `localStorage`。
- 安装脚本 `install.ps1`（离线安装到指定 profile，UTF-8 BOM 兼容 Windows PowerShell 5.1）。
