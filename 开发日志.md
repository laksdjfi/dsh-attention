# dsh-attention 开发日志

> 项目：DeepSeek Harness「确认提醒」插件
> 时间：2026-08-24
> 状态：**v0.2.0 已完成并测试成功**（用户确认：审批/提问/完成通知均可正常弹出）

---

## 一、需求

用户希望：当 DeepSeek Harness 需要用户**确认问题**（`ask_user_question`）或**取得权限**（审批/越权执行）时，能弹出提醒，避免用户没盯着页面而错过，Agent 一直阻塞等待。

要求先给方案、确认后再实施；工作区已有插件范例（`anydoc-project/dsh-anydoc`）可参照。

## 二、调研结论（DSH 机制）

- **审批机制**：`@deepseek-ai/dsh-user-approval` 提供 `ctx.approval.request()`，经 `approval/request` waterfall 事件分发；Web 端通过 `dsh-host-apiproxy` 应答，向浏览器发 `approval/requested` 帧。**服务自身绝不主动提示人类**，全靠应答者/UI。
- **提问机制**：`@deepseek-ai/dsh-tool-ask-user` 把 `ask_user_question` 工具参数转为 `ctx.userQuestions.ask()` 请求，Web 端发 `question/requested` 帧。
- **客户端运行时**（`dsh-client-runtime`）：两种帧都变成会话的 `PendingWait`（`kind: approval | question`，含 `respond()`），并汇总为会话列表快照中每项的 `pendingInteraction` 字段（`"approval"` / `"question"`）。
- **现有 UI**：审批卡片（`dsh-client-ui-conversation` 的 `ApprovalPanel`）与问题卡片（`dsh-client-ui-user-questions` 的 `QuestionComposer`）都只**内联渲染在聊天输入框区域**——用户不看页面就看不到。
- **客户端插件机制**（参照 dsh-anydoc）：`package.json` 声明 `dsh.bundle.patch`（挂进 profile 层栈）与 `dsh.client.inject`（注入浏览器模块）；浏览器模块用 `window.__ModuleLoader__.load({ id, factory })` 格式；插槽系统提供 `shell.overlay`（全局悬浮层）、`settings.section`（设置分区）等座位。
- **会话列表快照**：`ctx.sessions.list`（useSyncExternalStore 兼容）的 `getSnapshot()` 返回 `{ ids, byId, current, ... }`，`byId[id].pendingInteraction` 与 `displayTitle` 可直接用于监控；`ctx.sessions.select(id)` 可跳转会话。

## 三、方案（已与用户确认）

- **形态**：纯客户端插件 `dsh-attention`。
- **提醒通道**：用户选定只做**系统通知**（浏览器 `Notification` API；切窗口/后台标签页也能收到）。
- **交互**：点击通知 → 聚焦窗口 + 自动选中对应会话 → 落地到 DSH 内置的审批/提问卡片直接回答（提醒 + 跳转，不在弹窗内重复造回答 UI）。
- **防打扰**：页面聚焦且正在该会话时不重复提醒（内置卡片已可见）。
- **设置**：设置 →「确认提醒」分区：开关、通知权限授权、测试通知；偏好存 localStorage。

## 四、实现

目录：`D:\36699\Documents\deepseek harness\dsh-attention\`

| 文件 | 说明 |
|---|---|
| `package.json` | bundle patch + client inject 元数据；`type: module` |
| `cordis.patch.yml` | 把 bundle `dsh-attention` 挂进 profile 层栈 |
| `lib/index.js` | 服务端空挂载（仅日志一行） |
| `lib/client.js` | 核心：监控 `sessions.list` 的 `pendingInteraction` 差集 → 系统通知；点击跳转；设置面板 |
| `install.ps1` | 离线安装脚本（复制到 profile + 注册 bundles/dependencies） |
| `README.md` | 使用说明 |

关键逻辑（`lib/client.js`）：
- `startWatcher`：订阅 `sessions.list`，与 `seen` Map 做差集，只对新出现的 `approval`/`question` 发通知；会话恢复时解除去重。
- `fireNotification`：`new Notification('DeepSeek Harness · 需要确认', { body, tag, requireInteraction })`；`onclick` → `window.focus()` + `sessions.select(sessionId)`。
- 设置面板 `AttentionSettingsPanel` 注册到 `settings.section`（id: `attention`）。

## 五、安装与重启过程（有波折）

1. `install.ps1` 首次运行报错：文件为 UTF-8 无 BOM，Windows PowerShell 5.1 按 ANSI 解析中文导致语法错误 → 改为 **UTF-8 with BOM** 解决。
2. 范例脚本的 `Resolve-Path` 在目标目录不存在时会抛错（首次安装场景）→ 加 `Test-Path` 防护。
3. 复制到 `~/.dsh/profiles/web/node_modules/` 被沙箱拦截 → 以 `danger-full-access` 权限重试成功；profile 的 `package.json` 已注册 `dsh-attention`（dependencies + bundles）。
4. 第一次自动重启失败：**沙箱拦截 `taskkill`**（连不存在的 PID 都报 Access denied），旧服务进程（21:13 启动）未被杀死、端口被占，新服务 `EADDRINUSE` 退出——插件没生效。
5. 第二次以完整权限重启：`restart-dsh.ps1 -ReallyRun -DelaySec 45`，45 秒后杀掉旧监听进程；重启助手进程随服务树一起终止，状态日志中断，但服务最终由新进程（PID 6796，21:27 启动，晚于插件安装）接管。

## 六、验证结果

- ✅ `http://127.0.0.1:3080/plugins/dsh-attention/client.js` 返回 **200**，内容包含插件 id —— 客户端模块已被新服务物化并注入，**插件已加载**。
- ✅ 服务端日志无 bundle 加载错误。
- ⏳ 浏览器侧待人工确认：设置 →「确认提醒」分区是否出现；「授权系统通知」→「发送测试通知」是否弹出。

## 七、v0.2.0：完成通知

用户追加需求：后台任务跑完时也要弹通知。实现要点：

- **数据源**：DSH 运行时内置 `completedNotifications`（会话在**未被选中**期间跑完才点亮，用于侧边栏绿色"完成"标记），经会话列表快照暴露为 `byId[id].completed` —— 天然只覆盖后台任务，不会打扰正在盯着的会话。
- **逻辑**：在 `startWatcher` 中新增第二段差集监控（`completedSeen` Set）：`completed` 由假变真 → 弹「DeepSeek Harness · 任务完成」通知（`tag: dsh-attention:done:<id>`，点击跳转）；标记清除（用户选中/下次运行）后解除去重。
- **通知函数重构**：原 `fireNotification` 拆为通用 `showNotification({title, body, tag, onSelect})` + `firePendingNotification` + `fireCompletionNotification`。
- **设置**：新增 `notifyCompleted`（默认开），设置面板加对应开关；版本升至 `0.2.0`。
- **文件更新**：`lib/client.js`、`package.json`（0.2.0）、`README.md`（功能/原理/限制）、`CHANGELOG.md`（[0.2.0]）、本日志；重新打包 `dsh-attention-0.2.0.zip`（移除旧 0.1.0 包）。
- **待验证**：重新安装到 profile 并重启后，确认 `/plugins/dsh-attention/client.js` 内容含 `notifyCompleted`；浏览器侧验证「完成通知」开关与真实后台任务完成弹窗。

> **2026-08-24 验证结果（用户确认）**：测试成功 —— 设置「确认提醒」分区可用，通知权限授权正常，审批/提问/完成通知均可正常弹出，点击跳转正常。服务端端点 `/plugins/dsh-attention/client.js` 返回 200，内容含 `notifyCompleted` 与「任务完成」标题，v0.2.0 全链路闭环。

## 八、GitHub 上传需求变更（2026-08-24）

按工作区 `GitHub 上传需求文档模板.md`（上传人 laksdjfi）对插件做上传前整理：

- **元数据**：`package.json` 补充 `repository` / `homepage` / `bugs`（`https://github.com/laksdjfi/dsh-attention`）；`license: MIT` 与 `LICENSE`（`Copyright (c) 2026 laksdjfi`）一致。
- **剥离机器信息**：`install.ps1` 移除本机 `launcher\restart-dsh.ps1` 引用，改为通用"运行你的启动脚本或重启 dsh web"；README 此前已通用化。
- **整理工作区**：旧发布包 `dsh-attention-0.1.0.zip` 已删除；`.gitignore` 覆盖 `node_modules/`、`*.zip`、`*.log`。
- **同步与打包**：更新后的 `package.json` / `install.ps1` 已同步到已安装副本（仅元数据/脚本变更，无需重启）；曾重新打包 `dsh-attention-0.2.0.zip`。
- **用户决定：上传不需要下载包** —— 删除工作区中的 `dsh-attention-0.2.0.zip`，发布不附任何压缩包；`GitHub 上传需求文档模板.md` 已同步移除发布包相关条款（交付物清单、发布要求、验收标准、整理工作区）；`.gitignore` 保留 `*.zip` 忽略规则以防误传。
- **上传 CheckList 状态**：LICENSE 版权行 ✅ · repository/homepage ✅ · 版本/许可证一致 ✅ · 必传文件齐全 ✅ · 机器路径已清理 ✅ · 无发布包 ✅ · tag v0.2.0（待用户操作）⏳

## 九、待办

- [x] 浏览器侧功能验证（设置分区、通知权限、测试通知、真实审批/提问触发）—— 用户确认测试成功
- [x] 上传前整理（元数据、机器信息清理、旧包清理、移除发布包）—— 见第八节
- [ ] 用户操作：创建 GitHub 仓库 `dsh-attention`（归属 laksdjfi）→ 上传必传文件 → 打 tag `v0.2.0`（不附下载包）
- [ ] 可选后续：通知点击直接内嵌回答控件（需复用 PendingWait.respond 链路）、多通道提醒（应用内浮层/标题角标/声音）
